public with sharing class deleteContacts {
    public static void  deleteContacts( String ids ) {
     
            // Delete [Select id from Contacts__c where  id=:ids];
 
            // Delete [Select Id FROM CONTACT WHERE Id in :ContactsForDelitionID ];
    }
}
